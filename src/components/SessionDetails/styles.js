import styled from 'styled-components';
import Box from 'grommet/components/Box';

export const SessionDetailsLayout = styled(Box)`
  p {
    max-width: 250px;
    word-wrap: break-word;
    overflow: auto;
  }
`;
